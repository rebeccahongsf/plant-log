import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 30,
  },
  inputContainer: {
    display: 'flex',
    flexDirection: 'row',
    marginBottom: 25,
  },
  icon: {
    marginRight: 10,
  },
  inputText: {
    fontSize: 16,
  },
  button: {
    backgroundColor: '#ccc',
    padding: 5,
    width: '100%',
  },
});

export default styles;
